---
audience: [human, agent]
role: [user, developer]
topic: changelog
kind: reference
updated: 2026-05-24
---
# osiris-toolkit v0.1.0 Dev Log

## 2026-05-24 â€?Project Inception

**Decision**: Integrate three standalone OSIRIS tools (`osiris-deck-parser` v0.3.0, `osiris-data-lib` v0.1.0, `osiris-vis` v0.1.0) into a single package.

**Starting state:**
- Three independent packages with separate `pyproject.toml`, separate `.venv`, separate `src/` layouts
- `osiris-vis` already depended on `osiris-data-lib` via editable install
- No shared infrastructure between them
- Two modules had zero tests, one had hardcoded paths

**Design decisions:**
- Target: single installable package `osiris-toolkit`, CLI + Python API
- Architecture: `deck/` `io/` `sim/` `units/` `analysis/` `vis/` `workflow/` `sync/` `_generated/`
- Units driven by deck parser output, eliminating regex-based `omega_p0` extraction
- Declarative diagnostic type catalog (`DiagKind` dataclass) for auto-discovery
- English throughout (open-source project)
- MIT license target

**Architecture chosen:**

```
osiris-toolkit/
â”œâ”€â”€ deck/         Input deck lexer, parser, validator
â”œâ”€â”€ io/           ZDF binary format reader
â”œâ”€â”€ sim/          Simulation directory discovery, 12 diagnostic types
â”œâ”€â”€ units/        Normalized â†?physical unit conversion
â”œâ”€â”€ analysis/     Statistics, EMF energy/spectra, density profiles
â”œâ”€â”€ vis/          Plotting: fields, density, phasespace, k-space, scattering
â”œâ”€â”€ workflow/     YAML pipeline: deck â†?analyze â†?visualize
â”œâ”€â”€ sync/         Fortran source extractor (dev-time)
â””â”€â”€ _generated/   Auto-generated definitions
```

---

## 2026-05-24 â€?Phase 1: Basic Integration

**Completed:**
- Created `osiris-toolkit/` project skeleton with `pyproject.toml` (hatchling build)
- Migrated `io/zdf.py` from `osiris-data-lib` â€?ZDF binary reader, all 10 data types, English docstrings
- Migrated `sim/` â€?`diagnostics.py` (6 dataclasses), `simulation.py` (12 diagnostic accessors), `catalog.py` (declarative `DiagKind` table)
- Migrated `deck/` â€?10 source files (lexer, parser, AST, value parser, validator, cross-validator, reporter, schemas). All internal imports updated (`osiris_deck_parser` â†?`osiris_toolkit.deck`). Chinese â†?English.
- Created `units/params.py` â€?`SimulationParams` dataclass extracting `omega_p0`, `n0`, `gamma` from parsed deck
- Rewrote `units/converter.py` â€?removed regex-based `_parse_omega_p0_from_deck()`, removed `from_input_deck()` and `from_simulation()` factory methods. Now receives `SimulationParams` directly.
- Basic CLI: `osiris-toolkit deck parse/lint/validate`, `osiris-toolkit sim info/list`
- 16 tests passing

**Key design change**: The `UnitConverter` no longer parses input files with regex. Instead, `SimulationParams.from_deck(deck_dict)` extracts parameters from the already-parsed deck. This eliminates fragile regex and ensures the converter always uses accurate values from the full parser.

---

## 2026-05-24 â€?Phase 2: Sync Module

**Completed:**
- Implemented `sync/extractor.py` â€?regex-based Fortran source scanner. Parses:
  - `namelist /nl_name/` declarations â†?parameter names
  - Fortran type declarations (`integer ::`, `real(p_double) ::`, etc.)
  - Default value assignments (`var = value`)
  - `p_report_quants` character arrays â†?diagnostic quantity names
  - `call get_namelist()` â†?section-to-namelist mappings
  - Fortran continuation line merging (`&`)
- Implemented `sync/namelist.py` â€?generates `_generated/parameters.py`
- Implemented `sync/diagnostics.py` â€?generates `_generated/quantities.py`
- Implemented `sync/sections.py` â€?generates `_generated/sections.py`
- CLI command: `osiris-toolkit sync extract --osiris-path <path>`

**Extraction results:**
- 59 diagnostic quantities across 4 types: EMF(34), DENSITY(9), UDIST(12), CURRENT(4)
- 37 input sections with namelist mappings
- 493 parameters with Fortran types and default values

**Design note**: The sync module runs at development time only. Generated files are committed to the repo. At runtime, `osiris-toolkit` has zero dependency on `osiris-1.0.0/` Fortran source. When OSIRIS is upgraded, re-run the sync command and `git diff _generated/` to audit changes.

---

## 2026-05-24 â€?Phase 3: Analysis & Visualization

**Analysis layer (`analysis/`):**
- `stats.py` â€?Generic statistics: `describe()`, `mean()`, `std()`, `minmax()`, `rms()`, `total_energy()`, `lineout()`
- `emf.py` â€?`EMFAnalyzer`: field energy, total EM energy (EÂ²+BÂ²), FFT spectrum, Poynting flux (EÃ—B)
- `species.py` â€?`SpeciesAnalyzer`: density profile integration, total charge, energy spectrum histogram, temperature tensor
- `__init__.py` â€?`Analyzer` unified entry with `.emf` and `.species` sub-analyzers

**Visualization layer (`vis/`):**
- Migrated 8 modules from `osiris-vis`: `common.py`, `field.py`, `density.py`, `phasespace.py`, `composite.py`, `kspace.py`, `scattering.py`, `batch.py`
- Eliminated all hardcoded paths (was `D:\zty\Documents\myproject\pigsproject\...`)
- `load_sim()` now requires explicit path argument
- `batch.py` rewritten: delegates to other vis modules instead of reimplementing plotting logic inline
- All Chinese docstrings â†?English
- `VisEngine` unified entry with agent-friendly `plot(kind, **kwargs)` interface

**Design note**: Both `Analyzer` and `VisEngine` follow the same dispatch pattern â€?each diagnostic type gets a sub-accessor property (`.emf`, `.species`, etc.) plus a generic `plot()`/`describe()` method for programmatic/agent use.

---

## 2026-05-24 â€?Phase 4: Workflow & CLI

**Workflow layer (`workflow/`):**
- `PipelineContext` â€?shared state bag passed between steps
- `PipelineStep` â€?abstract base, `run(ctx) â†?ctx`
- `DeckParseStep`, `DeckValidateStep`, `SimLoadStep`, `AnalyzeStep`, `VisualizeStep`
- `Pipeline.from_yaml(path)` â€?YAML configuration parsing
- `quick_pipeline(deck_path, sim_path)` â€?minimal two-step pipeline

**CLI (6 command groups):**
- `deck parse/lint/validate` â€?input deck operations
- `sim info/list` â€?simulation browsing
- `vis plot` â€?single diagnostic frame
- `analyze describe` â€?statistical summary
- `run <workflow.yaml>` â€?pipeline execution
- `sync extract` â€?Fortran source extraction

**Design note**: The pipeline is a skeleton â€?`AnalyzeStep` and `VisualizeStep` perform minimal operations. The framework is designed to be extended with more sophisticated steps as the project matures.

---

## 2026-05-25 â€?Phase 5: Testing & Documentation

**Completed:**
- `README.md` â€?English, comprehensive: installation, quick start, architecture diagram, CLI reference
- 37 tests covering: deck parsing (5), unit conversion (11), statistics (6), workflow (8), sync extractor (7)
- `conftest.py` with shared fixtures
- Ruff lint configuration (line-length=120, exclude `_generated/`, ignore pep8-naming for ZDF conventions)

---

## 2026-05-25 â€?Task D-01, D-02: License Audit

**Investigation**: Traced the provenance of `io/zdf.py`. Discovered it is a direct derivative of ZPIC's `zdf.py` (Copyright 2017 Instituto Superior Tecnico, AGPL v3).

**Key findings:**
- `osiris-1.0.0/source/zdf/zdf.py` carries a full AGPL v3 header with IST copyright
- Our version had removed the original copyright notice â€?a compliance issue under AGPL v3 Â§4
- OSIRIS itself is AGPL v3, same license as ZPIC, same research group (IST/UCLA). No conflict there â€?OSIRIS never needed to change license.
- We need MIT, so AGPL code is incompatible â€?must be removed or rewritten.
- All other modules are original work (simulation.py, converter.py, deck parser, sync modules)
- Parameter schemas contain factual extractions (parameter names, types, defaults), not creative expression
- The ZDF format specification (`zdf/README.md`) has no license statement

**Decision**: Path 3 â€?clean-room rewrite of the ZDF reader from the format specification. This enables MIT licensing for the entire project.

**Three paths considered:**

| Path | Approach | Outcome |
|------|----------|---------|
| 1 | Claim non-derivative | Rejected â€?zdf.py is clearly a direct port |
| 2 | Keep AGPL, fix compliance | Viable but restricts downstream use |
| 3 | Clean-room rewrite from spec | Chosen â€?enables MIT, eliminates copyleft |

---

## 2026-05-25 â€?Task D-03, D-04: ZDF Clean-Room Rewrite

**Approach**: Implement a ZDF binary reader using ONLY:
1. The ZDF format specification (`zdf/README.md` â€?no license, functional description)
2. Empirical verification against real ZDF binary files

**Empirical analysis performed:**
- Read `zdftest.zdf` binary header â†?verified magic `ZDF1`, record type IDs
- Read large E-field file (6656Ã—9888 cells, 517 chunk records) â†?verified chunked dataset protocol
- Verified record type ID values: `0x00030000=TYPE, 0x00210000=GRID, 0x00200000=ITERATION, 0x00100000=DATASET`

**Old architecture (deleted):**
- `io/zdf.py` â€?`ZDFfile` class, stateful file handle, 838 lines, derived from AGPL code

**New architecture:**

| File | Purpose | Lines |
|------|---------|-------|
| `io/_format.py` | Format constants: magic, record type IDs, dtype maps | ~100 |
| `io/_types.py` | Lightweight dataclasses: ZdfRecord, ZdfGridInfo, etc. | ~80 |
| `io/_reader.py` | Stateless pure functions: `read_grid()`, `read_particles()`, `read_tracks()` | ~530 |
| `io/_parallel.py` | Parallel batch I/O: `read_many()` with ThreadPoolExecutor | ~90 |
| `io/__init__.py` | Unified exports | ~50 |

**Key design decisions:**
- All functions are stateless (openâ†’readâ†’close). No shared state between calls.
- Inherently thread-safe â€?each call opens its own file handle
- Pickle-safe â€?module-level functions, no instance methods
- `_parallel.read_many()`: preserves input order, throttles via `max_workers`, cancel-on-error
- ZDF format constants derived from the spec + verified against real binary files
- Array reshaping: Fortran (column-major) order in file â†?C (row-major) numpy order
- `_read_file_type()` helper correctly handles the first (TYPE) record â€?reads record header, then parses the STRING data from the record body

**Compliance verification:**
- Zero code derived from `osiris-1.0.0/source/zdf/zdf.py`
- Zero code derived from `zdf.c` / `zdf.h`
- Record type IDs are functional constants (binary protocol), not creative expression
- ZDF format attribution preserved in `_format.py` and `__init__.py` docstrings

---

## 2026-05-25 â€?Task D-05: MIT License Switch

**Completed:**
- Deleted `io/zdf.py` (AGPL-derived code)
- Updated `sim/simulation.py`: 4 internal ZDF reader methods adapted to new API
- `LICENSE`: AGPL-3.0 â†?MIT
- `pyproject.toml`: `license = { text = "MIT" }`, classifier `MIT License`
- `README.md`: MIT license section + ZDF format attribution statement
- All 37 tests pass, Ruff clean

---

## 2026-05-25 â€?GitHub Setup

**Repository**: `https://github.com/wulnkkk/osiris-toolkit`

**CI issues and fixes:**

1. `uv pip install --system` rejected on Ubuntu runner
   - Fix: replaced with `uv sync --dev`
2. `uv sync --dev` not finding `[project.optional-dependencies]`
   - Root cause: `--dev` reads `[dependency-groups]` (PEP 735), not `[project.optional-dependencies]`
   - Fix: migrated `pyproject.toml` from `[project.optional-dependencies]` to `[dependency-groups]`
   - Also updated `README.md`: `uv pip install -e ".[dev]"` â†?`uv sync --dev`
3. 4 tests failed in CI â€?referenced external files outside the repo
   - `test_parse_base_2d`, `test_parse_base_3d`, `test_lint_base_2d` â†?`osiris-1.0.0/decks/test/`
   - `test_from_yaml` â†?`osiris-deck-parser/inputtest/Au.in`
   - Fix: bundled `tests/fixtures/minimal.in` (11-section valid deck) for self-contained tests
   - External-file tests kept with `pytest.skip()` guards for local development
4. YAML test Windows path issue
   - Windows temp paths contain backslashes â†?YAML interprets `\U`, `\t` as escapes
   - Fix: `deck_path.replace("\\", "/")` before embedding in YAML content

**Final CI** (`.github/workflows/ci.yml`):
- Matrix: Python 3.10, 3.11, 3.12 on ubuntu-latest
- `astral-sh/setup-uv@v5` with `python-version` parameter
- `uv sync --dev`, `uv run ruff check src/`, `uv run pytest tests/ -v`
- All 37 tests pass on all 3 Python versions

---

## Current State Summary

| Metric | Value |
|--------|-------|
| Package | `osiris-toolkit` v0.1.0 |
| License | MIT |
| Python | â‰?.10 |
| Dependencies | numpy, matplotlib, click, pyyaml |
| Source files | 49 Python files |
| Test files | 6 files, 37 tests |
| CLI groups | 6 (deck, sim, vis, analyze, run, sync) |
| Diagnostic types | 12 (EMF, DENSITY, CELL_AVG, UDIST, PHASESPACE, RAW, TRACKS, CURRENT, ION, WALL, CHARGE_CONS, HISTORY) |
| Auto-extracted | 59 quantities, 37 sections, 493 parameters |
| ZDF reader | Clean-room, stateless, parallel-ready (4 files, ~800 lines) |
| CI | GitHub Actions, 3 Python versions, ruff + pytest |
| Language | English throughout |
| Hardcoded paths | Zero |

## Future Directions

- Extend `AnalyzeStep` and `VisualizeStep` with full analysis routines
- Add write support to ZDF reader for generating diagnostic files
- Integration with simulation job submission (Slurm/PBS)
- Interactive dashboard (Streamlit or Jupyter widget)
- PyPI publication
